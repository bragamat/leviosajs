#! /usr/bin/env node
const chalk = require("chalk");
const figlet = require("figlet");
const bundleComponent = require("./helpers");

const run = async () => {
  const credentials = await bundleComponent;
};

run();
